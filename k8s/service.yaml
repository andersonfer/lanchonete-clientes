# Service LoadBalancer para Clientes
apiVersion: v1
kind: Service
metadata:
  name: clientes-service
  labels:
    app: clientes
    component: service
spec:
  type: LoadBalancer
  selector:
    app: clientes
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
